[project]
name = "latchgate-agent-loop"
version = "0.1.0"
requires-python = ">=3.10"
dependencies = [
    "latchgate",
    "anthropic>=0.40",
]

[dependency-groups]
dev = [
    "black>=26.3.1",
]


[tool.uv.sources]
latchgate = { path = "../../sdk/python", editable = true }
