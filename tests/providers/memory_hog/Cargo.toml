[package]
name        = "latchgate-fixture-memory-hog"
version.workspace = true
edition.workspace = true
description = "Adversarial WASM fixture: allocates until OOM — verifies memory limit enforcement."

[lib]
crate-type = ["cdylib"]

[dependencies]
wit-bindgen = { workspace = true }

[package.metadata.component]
package = "latchgate:provider"

[package.metadata.component.target]
world = "provider"
path  = "../../../providers/wit"
