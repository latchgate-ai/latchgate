[package]
name        = "latchgate-fixture-infinite-loop"
version.workspace = true
edition.workspace = true
description = "Adversarial WASM fixture: tight infinite loop — verifies epoch-based wall-clock timeout."

[lib]
crate-type = ["cdylib"]

[dependencies]
wit-bindgen = { workspace = true }

[package.metadata.component]
package = "latchgate:provider"

[package.metadata.component.target]
world = "provider"
path  = "../../../providers/wit"
