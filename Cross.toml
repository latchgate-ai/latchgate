# Cross-compilation configuration.
#
# No pre-build steps required: the release workflow strips the
# .cargo/config.toml aarch64 linker overrides before invoking
# cross, so the default GCC cross-toolchain is used as-is.
[target.aarch64-unknown-linux-gnu]
